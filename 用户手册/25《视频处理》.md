# 视频处理——录制与播放

<style>
.two-col {
  display: flex;
  gap: 2em;
}
.two-col > div {
  flex: 1;
  width: 50%;
}
</style>

本章介绍黑板的视频录制和播放功能。录制功能可将画布操作过程录制成 WebM 视频文件，播放功能支持在黑板内直接播放视频。

---

<div class="two-col">
<div>

## 录制视频

### 作用
将黑板画布上的操作过程录制成视频文件（WebM 格式），适合录制教学讲解、解题过程等。

### 操作步骤
1. 点击右侧菜单 **视频 → 录制视频**
2. 在弹出的录制面板中设置参数：
   - **帧率**：10 / 15 / 24 / 30 fps（帧率越高画面越流畅，文件越大）
   - **录制音频**：开启/关闭麦克风录音
   - **音频来源**：麦克风（录制说话声）/ 系统音频（录制电脑内部声音）
3. 点击 **开始录制** 按钮
4. 录制过程中：
   - 画布右上角显示红色录制指示器（闪烁红点 + 计时器）
   - 可关闭录制面板，录制在后台继续
   - 可随时打开面板 **暂停** / **继续** / **停止**
5. 点击 **停止录制** 按钮
6. 弹出保存对话框，选择保存位置和文件名

### 录制范围
只录制画布<strong>当前视口内的可见区域</strong>。超出屏幕范围的画布内容不会被录制。

### 输出格式
- 格式：WebM（VP9 编码）
- 默认文件名：`课程录制_YYYY-MM-DD HH-mm-ss.webm`

</div>
<div>

## 播放视频

### 作用
在黑板内播放视频文件，支持 WebM / MP4 / MOV 格式，可拖动位置、调整大小。

### 打开视频的方式

**方式一：通过菜单打开**
1. 点击右侧菜单 **视频 → 播放视频**
2. 在弹出的视频播放器中点击 **选择视频文件**
3. 选择本地视频文件

**方式二：直接打开视频文件**
1. 点击左侧菜单 **文件夹 → 打开本地文件**（或打开百度网盘文件）
2. 选择 .webm / .mp4 / .mov 文件
3. 视频自动在播放器中打开

**方式三：在最近文件列表中点击**
如果之前打开过 .webm 文件，可在左侧文件夹菜单的最近文件列表中直接点击打开。

### 播放器操作
- **播放/暂停/进度条**：标准视频控件
- **全屏**：点击标题栏全屏按钮或双击视频画面
- **拖动**：按住标题栏拖动播放器位置
- **调整大小**：拖动播放器右下角调整窗口大小
- **关闭**：点击右上角关闭按钮

### 支持的格式
| 格式 | 支持情况 |
|------|---------|
| WebM | 完全支持（推荐） |
| MP4 | 仅 H.264 (AVC) 编码 |
| MOV | 仅 H.264 (AVC) 编码 |

> 非标准编码的 MP4/MOV 文件无法在黑板内播放，建议使用系统播放器打开。

</div>
</div>

<hr>
<div class="two-col">
<div>

## 录制注意事项

### 录制前准备
- 确保画布已打开（新建黑板或打开文件）
- 如需录制音频，确保麦克风权限已授权
- 系统音频录制需要浏览器/系统支持（Tauri 桌面版支持更完善）

### 录制过程中
- 可以在黑板中正常操作（绘图、翻页、切换工具等），所有操作都会被记录
- 翻页操作也会被录制（切换页面的过程会记录在视频中）
- 录制的视频不含录制面板本身和顶部指示器

### 性能建议
- 帧率建议 15 fps（教学录制足够清晰流畅）
- 高帧率（24/30 fps）适合录制动画或快速操作
- 录制时关闭不必要的后台程序以保证流畅度

</div>
<div>

## 录制指示器

录制开始后，画布右上角会出现一个半透明的录制指示器：

- **红点闪烁** + "录制中" + 计时器（如 03:25）
- 暂停时红点停止闪烁，显示"已暂停"
- 点击指示器可快速打开录制面板

指示器本身不会被录制到视频中。

## 视频文件管理

录制产生的 .webm 文件：
- 可随后在黑板中打开播放（通过打开本地文件）
- 可作为教学资源上传到百度网盘
- 可导入到其他页面作为注释（通过打开本地文件方式）
- 可在文件管理器中直接拖入黑板播放

## 视频转换

如需将录制视频转换为其他格式，可使用第三方工具（如 FFmpeg）。黑板内置了简单的视频格式检测，播放时自动选择最佳解码方式。

</div>
</div>
